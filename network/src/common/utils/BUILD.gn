import("files.gni")

config("common_utils_config") {
  include_dirs = ["include"]
}

source_set("common_utils") {
  sources = common_utils_sources

  include_dirs = ["src"]

  public_configs = [
    ":common_utils_config"
  ]
}
import("files.gni")

config("common_utils_config") {
  include_dirs = ["include"]
}

component("common_utils") {
  _visibility = true

  sources = common_utils_sources

  include_dirs = ["src"]

  public_configs = [
    ":common_utils_config"
  ]
}
import("files.gni")

config("common_os_config") {
  include_dirs = ["include"]
}

component("common_os") {
  _visibility = true

  sources = common_os_sources

  public_configs = [
    ":common_os_config"
  ]
}
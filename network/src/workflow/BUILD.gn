import("//gn/tools.gni")

import("files.gni")

config("workflow_config") {
  include_dirs = ["include"]
}

component("workflow") {
  _visibility = true

  sources = workflow_sources
  deps = [
    "//src/common:common",
    "//src/common_gui:common_gui",
    ":workflow_moc",
  ]

  include_dirs = ["src"]

  configs = [
    "//gn:qt_config"
  ]

  public_configs = [
    ":workflow_config"
  ]
}

generate_moc("workflow_moc") {
  sources = workflow_sources
}
